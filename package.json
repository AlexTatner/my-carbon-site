{
  "name": "gatsby-starter-carbon-theme",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "scripts": {
    "build": "gatsby build",
    "dev": "gatsby develop",
    "dev:clean": "gatsby clean && gatsby develop",
    "serve": "gatsby serve",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public"
  },
  "dependencies": {
    "gatsby": "^2.13.39",
    "gatsby-theme-carbon": "^1.6.1",
    "react": "^16.9.0",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "gh-pages": "^2.1.1"
  }
}
